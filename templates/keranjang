{% extends 'base.html' %}
{% block title %}Keranjang - P-Njemin{% endblock %}
{% block content %}
<div style="max-width:700px;margin:0 auto;padding:1.5rem">
    <div class="page-header">
        <h1><i class="fas fa-shopping-cart" style="color:var(--primary)"></i> Keranjang</h1>
        <p>Barang yang ingin kamu pinjam</p>
    </div>

    {% if items %}
    <div class="card">
        {% for b in items %}
        <div class="barang-list-item" style="display:flex;align-items:center;gap:1rem;padding:1rem;border-bottom:1px solid var(--border)">
            <div style="width:70px;height:70px;flex-shrink:0;border-radius:var(--radius-sm);overflow:hidden;background:#eee;display:flex;align-items:center;justify-content:center">
                {% if b.foto %}
                <img src="{{ b.foto | foto_src }}" style="width:70px;height:70px;object-fit:cover">
                {% else %}
                <i class="fas fa-box" style="color:#bbb"></i>
                {% endif %}
            </div>
            <div style="flex:1">
                <div style="font-weight:700;font-size:0.95rem">{{ b.nama_barang }}</div>
                <div style="font-size:0.8rem;color:var(--text-muted)">{{ b.nama_pemilik }} &middot; {{ b.lokasi or '-' }}</div>
                <div style="font-weight:700;color:var(--primary);margin-top:0.2rem">Rp {{ "{:,.0f}".format(b.harga_sewa|int) }}/hari</div>
            </div>
            <div style="display:flex;flex-direction:column;gap:0.5rem;align-items:flex-end">
                <a href="{{ url_for('detail_barang', id=b.id) }}" class="btn btn-primary btn-sm">
                    <i class="fas fa-eye"></i> Lihat
                </a>
                <form method="post" action="{{ url_for('keranjang_hapus', id_barang=b.id) }}">
                    <button type="submit" style="background:transparent;border:none;color:#e74c3c;cursor:pointer;font-size:0.8rem;padding:0.3rem 0.5rem">
                        <i class="fas fa-trash"></i> Hapus
                    </button>
                </form>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="card" style="text-align:center;padding:3rem;color:var(--text-muted)">
        <i class="fas fa-shopping-cart" style="font-size:2.5rem;opacity:0.3;display:block;margin-bottom:0.8rem"></i>
        Keranjang kamu masih kosong.<br>
        <a href="{{ url_for('home') }}" class="btn btn-primary btn-sm" style="margin-top:1rem">Cari Barang</a>
    </div>
    {% endif %}
</div>
{% endblock %}